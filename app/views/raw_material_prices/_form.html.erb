<%= form_with(model: raw_material_price, local: true) do |form| %>
  <% if raw_material_price.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(raw_material_price.errors.count, "error") %> prohibited this raw_material_price from being saved:</h2>

      <ul>
        <% raw_material_price.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.hidden_field :raw_material_id,value: params[:rm_id] %>

  <div class="form-group">
    <%= form.label :price %>
    <%= form.text_field :price,class:"form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :price_date %>
    <%= form.date_select :price_date %>
  </div>

  <div class="form-group">
    <%= form.submit "Add Price",class:"btn btn-success" %>
  </div>
<% end %>
